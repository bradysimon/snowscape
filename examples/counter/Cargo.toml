[package]
name = "counter"
version = "0.1.0"
edition = "2024"
publish = false

[lib]
name = "counter"
path = "src/lib.rs"

[dependencies]
snowscape = { path = "../.." }
iced.workspace = true
tokio = { workspace = true, features = ["time"] }

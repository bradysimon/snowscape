# Change Log

All notable changes to the "snowscape-previews" extension will be documented in this file.

## [Unreleased]

### Added
- Initial release
- Code lens provider for `#[snowscape::stateless]` and `#[snowscape::stateful]` attributes
- "Run Preview" button above preview functions
- Terminal integration to run preview commands
- Auto-detect cargo workspace packages and use `-p <package>` flag when needed
- Configuration options for preview command and enabling/disabling code lenses
